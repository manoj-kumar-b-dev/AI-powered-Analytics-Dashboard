# Services Layer

- **Purpose**: Core business logic of the application (Phases 2-10 will live here).
- **Belongs here**: Domain algorithms, business rule execution, calculations, and orchestrations.
- **Does NOT belong here**: Direct Express request/response objects (HTTP objects), raw database queries (repositories only), and route handlers.
