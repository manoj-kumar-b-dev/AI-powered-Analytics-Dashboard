# Config Layer

- **Purpose**: Manage application configuration, environment variables, database connections, and global constants.
- **Belongs here**: Configuration modules, `.env` parsers, database connection initializers, and static global constants.
- **Does NOT belong here**: Business logic, database schemas/models, routing, middleware, and request validation.
