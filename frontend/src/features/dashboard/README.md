# Dashboard Feature

- **Purpose**: Displays the main workspace, handles drag-and-drop grid layouts, and orchestrates widget configurations.
- **Belongs here**: Grid layouts, widget card wrappers, layout persistence triggers, and dashboard configuration modals.
- **Does NOT belong here**: Generic UI primitives (like buttons or inputs) and raw upload drops.
